import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 10,
        backgroundColor: '#fff',
        flex: 1,
    },
    text: {
        fontSize: 20,
        marginBottom: 10,
    },
    footer: {
        marginTop: 20
    }
})